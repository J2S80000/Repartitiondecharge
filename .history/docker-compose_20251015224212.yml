version: '3'
services:
  principal_a:
    image: "mongo-start"
    build: principal_a
    ports:
      - "27017:27017"
    volumes:
      - ./mongo.conf:/etc/mongo.conf
      - ./data/principal_a:/data/db
    command: ["mongod", "--config", "/etc/mongo.conf"]